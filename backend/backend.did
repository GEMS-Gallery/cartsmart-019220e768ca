type Item = 
 record {
   completed: bool;
   id: nat;
   "text": text;
 };
service : {
  addItem: (text) -> (nat);
  deleteItem: (nat) -> (bool);
  getItems: () -> (vec Item) query;
  getSuggestions: () -> (vec text) query;
  toggleItem: (nat) -> (bool);
}
